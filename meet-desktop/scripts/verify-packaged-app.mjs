import fs from 'node:fs';
import path from 'node:path';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';

const appPath=process.argv[2];
if(!appPath)throw new Error('Usage: node verify-packaged-app.mjs <DominionStar Meet.app>');
const absolute=path.resolve(appPath);
assert(fs.existsSync(absolute),'Packaged .app does not exist.');
const resources=path.join(absolute,'Contents','Resources');
const asarPath=path.join(resources,'app.asar');
assert(fs.existsSync(asarPath),'Packaged app.asar is missing.');
assert(fs.existsSync(path.join(resources,'branding','dominionstar-logo.jpeg')),'Packaged app must include the real DominionStar logo resource.');

const listing=execFileSync(process.execPath,[path.resolve('node_modules/@electron/asar/bin/asar.js'),'list',asarPath],{encoding:'utf8'});
const required=[
  '/src/main.mjs','/src/auth-service.mjs','/src/meeting-service.mjs','/src/share-service.mjs','/src/share-source-authority.mjs','/src/preload.cjs',
  '/ui/index.html','/ui/app.js','/ui/auth-password.js','/ui/media-controller.js','/ui/video-effects.js','/ui/av-settings.js','/ui/av-settings.css','/ui/meeting-parity.js','/ui/meeting-parity.css','/ui/meeting-features.js','/ui/meeting-features.css','/ui/meeting-captions.js','/ui/meeting-captions.css','/ui/zoom-behavior.js','/ui/zoom-behavior.css','/ui/participant-controls.js','/ui/participant-controls.css','/ui/meeting-notifications.js','/ui/meeting-notifications.css','/ui/preferences.js','/ui/personal-room.js','/ui/personal-room.css','/ui/schedule-controller.js','/ui/schedule.css','/ui/share-controller.js','/ui/share-integration.js','/ui/share-annotation.js','/ui/share-picker.html','/ui/presenter-toolbar.html',
  '/ui/webrtc-controller.js','/ui/webrtc.css','/ui/diagnostics.js','/ui/diagnostics.css','/package.json'
];
for(const item of required)assert(listing.includes(item),`Packaged ASAR is missing ${item}`);
for(const forbidden of ['/desktop/','/desktop 2/','/meet-home/','/meet-login/','/assets/js/meeting-engine.js'])assert(!listing.includes(forbidden),`Legacy runtime leaked into packaged ASAR: ${forbidden}`);

const unpackDir=path.resolve('.package-audit');
fs.rmSync(unpackDir,{recursive:true,force:true});
execFileSync(process.execPath,[path.resolve('node_modules/@electron/asar/bin/asar.js'),'extract',asarPath,unpackDir]);
const read=(...parts)=>fs.readFileSync(path.join(unpackDir,...parts),'utf8');
const main=read('src','main.mjs'),auth=read('src','auth-service.mjs'),meeting=read('src','meeting-service.mjs'),preload=read('src','preload.cjs'),share=read('src','share-source-authority.mjs'),shareService=read('src','share-service.mjs');
const appUi=read('ui','app.js'),media=read('ui','media-controller.js'),videoEffects=read('ui','video-effects.js'),authPassword=read('ui','auth-password.js'),html=read('ui','index.html'),av=read('ui','av-settings.js'),parity=read('ui','meeting-parity.js'),parityCss=read('ui','meeting-parity.css'),features=read('ui','meeting-features.js'),captions=read('ui','meeting-captions.js'),captionsCss=read('ui','meeting-captions.css'),zoomBehavior=read('ui','zoom-behavior.js'),zoomCss=read('ui','zoom-behavior.css'),participantControls=read('ui','participant-controls.js'),participantCss=read('ui','participant-controls.css'),meetingNotifications=read('ui','meeting-notifications.js'),meetingNotificationsCss=read('ui','meeting-notifications.css'),preferences=read('ui','preferences.js'),personal=read('ui','personal-room.js'),schedule=read('ui','schedule-controller.js'),scheduleCss=read('ui','schedule.css'),shareController=read('ui','share-controller.js'),annotation=read('ui','share-annotation.js'),presenterHtml=read('ui','presenter-toolbar.html'),presenterJs=read('ui','presenter-toolbar.js'),webrtc=read('ui','webrtc-controller.js');
assert(participantControls.includes("type==='host:view-layout'")&&participantControls.includes('authorizedSender(detail.fromParticipantId)'),'Packaged meeting-wide View changes must verify host/co-host sender authority.');
assert(parity.includes("applyAll.dataset.applyViewEveryone='1'")&&parity.includes("desktop.meeting.sendSignal(p.participantId,'host:view-layout',payload)"),'Packaged host/co-host View menu must broadcast Apply View to Everyone through meeting signaling.');
assert(parity.includes("window.addEventListener('dominion:host-view-layout'")&&parity.includes('applyViewMode(mode)'),'Packaged clients must apply authorized host View broadcasts through the local View engine.');
assert(parity.includes('spotlightParticipantId?q(`#participantVideoDock .remote-peer-tile[data-peer-id="${CSS.escape(spotlightParticipantId)}"]`)')&&parity.includes("featured?.classList.add('share-featured')"),'Packaged Side-by-side Speaker must feature Spotlight for Everyone before automatic active speaker.');
assert(parityCss.includes('button[data-apply-view-everyone]'),'Packaged host Apply View action must include dedicated styling.');

assert(main.includes("loadFile(path.join(uiDir,'index.html'))"),'Packaged desktop must launch the local Home file.');
assert(!main.includes('dominionstarld.com'),'Packaged desktop must not launch the public website.');
assert(main.includes('systemPreferences.getMediaAccessStatus(kind)'),'Packaged desktop must include native macOS media permission authority.');
assert(main.includes('powerMonitor')&&main.includes("powerMonitor.on('suspend'")&&main.includes("powerMonitor.on('resume'")&&preload.includes("power:Object.freeze({onChanged:"),'Packaged desktop must bridge native sleep/wake lifecycle through preload.');
assert(media.includes('async recoverAfterResume()'),'Packaged media authority must repair dead camera/mic tracks after wake without resetting user preferences.');
assert(webrtc.includes('async function handlePowerEvent(event={})')&&webrtc.includes("type==='resume'||type==='unlock-screen'")&&webrtc.includes('await window.DominionMediaController?.recoverAfterResume?.()'),'Packaged WebRTC must recover the same live session after sleep/unlock.');
assert(preload.includes('touchPresence:')&&main.includes("'meeting:touch-presence'")&&meeting.includes('meet_v2_touch_presence'),'Packaged desktop must wire presence heartbeat through preload, IPC, and meeting service.');
assert(webrtc.includes('async function touchPresence(force=false)')&&webrtc.includes('lastPresenceTouchAt'),'Packaged WebRTC must heartbeat the existing participant identity without creating a new join.');
assert(webrtc.includes('if(!current.has(id))closePeer(id)'),'Packaged roster reconciliation must remove ghost peer tiles.');
assert(main.includes("ipcMain.handle('notifications:meeting'")&&main.includes('new Notification('),'Packaged desktop must provide native background meeting notifications.');
assert(main.includes("ipcMain.handle('notifications:set-waiting-count'")&&main.includes('app.dock.setBadge')&&main.includes("app.dock.bounce('informational')")&&main.includes('mainWindow.flashFrame'),'Packaged desktop must expose native Waiting Room badge and attention behavior while unfocused.');
assert(preload.includes("notifications:Object.freeze({showMeeting:")&&preload.includes('setWaitingCount:'),'Packaged renderer must use a narrow notification bridge for alerts and Waiting Room badge state.');
assert(meetingNotifications.includes("version:'1.1.0'")&&meetingNotifications.includes("ctx?.setSinkId&&speakerId"),'Packaged meeting alerts must route to the selected speaker where supported.');
assert(meetingNotifications.includes("function chat(name='Participant')")&&features.includes("DominionMeetingNotifications?.chat?.(message.name||'Participant')"),'Packaged incoming chat must use the shared notification sound authority.');
assert(main.includes("permissionStatus('screen')"),'Packaged desktop must check Screen Recording permission before sharing.');
assert(auth.includes("flowType:'pkce'"),'Packaged Google auth must use PKCE.');
assert(auth.includes("CALLBACK_HOST='127.0.0.1'"),'Packaged auth must use the loopback callback.');
assert(auth.includes('client.auth.signInWithPassword'),'Packaged app must include email/password sign-in.');
assert(preload.includes('signInPassword:')&&authPassword.includes('auth.signInPassword'),'Packaged renderer must expose email/password only through narrow native auth IPC.');
assert(preload.includes('brand:Object.freeze({logoUrl})'),'Packaged bridge must expose the real DominionStar logo resource.');
assert(meeting.includes("/^\\d{3,7}$/"),'Packaged meeting authority must enforce 3–7 digit passcodes.');
assert(meeting.includes("/^\\d{10,11}$/"),'Packaged meeting authority must accept Personal and generated Meeting ID lengths.');
assert(!meeting.includes('.slice(0,7)'),'Packaged meeting authority must reject overlong passcodes instead of truncating them.');
assert(!meeting.includes('.slice(0,11)'),'Packaged meeting authority must reject overlong Meeting IDs instead of truncating them.');
assert(meeting.includes("roomCode:'',passcode:'',title:''"),'Packaged meeting context must retain Meeting ID and passcode.');
for(const bridge of ['personalRoom:','updatePersonalRoom:','startPersonalRoom:','schedule:','listSchedules:','startSchedule:','cancelSchedule:'])assert(preload.includes(bridge),`Packaged bridge missing ${bridge}`);
assert(main.includes("'meeting:personal-room'")&&main.includes("'meeting:schedule'")&&main.includes("'meeting:start-schedule'"),'Packaged main process must own Personal Room and schedule IPC.');
assert(media.includes("deviceId:id?{ideal:id}:undefined"),'Packaged camera authority must use resilient soft device preference.');
assert(media.includes("const candidates=unique([preferredId,...catalog.map(item=>item.id)])"),'Packaged camera authority must fall back to another available device.');
assert(authPassword.includes("script.src='./video-effects.js'"),'Packaged renderer must load the isolated video effects processor.');
assert(videoEffects.includes('new FaceDetector')&&videoEffects.includes('canvas.captureStream?.(30)')&&videoEffects.includes('cropForFrame'),'Packaged auto framing must create a real processed camera stream.');
assert(videoEffects.includes('state.backgroundBlur')&&videoEffects.includes('compositePerson')&&videoEffects.includes('createRadialGradient')&&videoEffects.includes('blurPx'),'Packaged background blur must preserve a feathered foreground person region and blur the outgoing background.');
assert(videoEffects.includes('setAppearance')&&videoEffects.includes('state.touchUp')&&videoEffects.includes('state.portraitLight'),'Packaged touch-up and portrait lighting must be part of the outgoing processed camera path.');
assert(videoEffects.includes('state.denoise')&&videoEffects.includes('historyWeight')&&videoEffects.includes('state.previousFrame')&&videoEffects.includes('state.currentFrame'),'Packaged outgoing video de-noise must use reusable temporal frame buffers after final rendering.');
assert(videoEffects.includes('setVirtualBackground')&&videoEffects.includes('drawVirtualBackground')&&videoEffects.includes('setBackgroundPersistence'),'Packaged camera processor must implement virtual background rendering and persistence.');
assert(videoEffects.includes('compositePerson')&&videoEffects.includes('createRadialGradient')&&videoEffects.includes("globalCompositeOperation='destination-in'"),'Packaged foreground compositor must feather presenter edges.');
assert(videoEffects.includes('updatePerformanceGovernor')&&videoEffects.includes("performanceMode==='balanced'")&&videoEffects.includes('renderAvgMs>27'),'Packaged effects processor must include adaptive performance fallback.');
assert(videoEffects.includes("state.denoise&&state.performanceMode==='full'")&&videoEffects.includes('Math.min(16,requestedBlur)'),'Packaged balanced effects mode must reduce expensive processing first.');
assert(videoEffects.includes('clearTransientMeetingEffects')&&webrtc.includes('clearTransientMeetingEffects?.()'),'Packaged current-meeting background must clear when WebRTC meeting lifecycle ends.');
assert(av.includes('Auto framing')&&av.includes('Auto framing strength'),'Packaged video settings must expose auto framing controls.');
assert(av.includes('Blur my background')&&av.includes('Background blur strength'),'Packaged video settings must expose background blur controls.');
assert(av.includes('Optimize outgoing video with de-noise')&&av.includes('Video de-noise strength'),'Packaged advanced video settings must expose outgoing de-noise controls.');
assert(av.includes('Virtual background')&&av.includes('Dominion Aurora')&&av.includes('Studio')&&av.includes('Custom image'),'Packaged video settings must expose built-in and custom virtual backgrounds.');
assert(av.includes('Keep virtual background for')&&av.includes('Current meeting only'),'Packaged virtual backgrounds must expose persistence scope.');
for(const script of ['./av-settings.js','./meeting-parity.js','./meeting-features.js','./zoom-behavior.js','./participant-controls.js','./meeting-notifications.js','./preferences.js','./personal-room.js','./schedule-controller.js'])assert(html.includes(`<script src=\"${script}\"></script>`),`Packaged Home must load ${script}.`);
for(const style of ['./schedule.css','./personal-room.css'])assert(html.includes(`<link rel=\"stylesheet\" href=\"${style}\">`),`Packaged Home must load ${style}.`);
assert(!html.includes('aria-label="Search"')&&!html.includes('data-section="contacts"')&&!html.includes('id="contactsSection"'),'Packaged Home must not contain dead Search or Contacts chrome.');
assert(!html.includes('will live here')&&!html.includes('wired after'),'Packaged Home must not contain developer placeholder copy.');
assert(personal.includes('Use Personal Meeting ID')&&personal.includes('useForInstant'),'Packaged New Meeting must support Personal Meeting ID behavior.');
assert(appUi.includes("['waiting_host','waiting'].includes(response.state)")&&appUi.includes('Waiting for the host to start this meeting')&&appUi.includes('You are in the Waiting Room'),'Packaged join UI must distinguish pre-host waiting from Waiting Room admission.');
assert(personal.includes('pattern="[0-9]{3,7}"')&&personal.includes('maxlength="7"'),'Packaged Personal Room editor must enforce 3–7 digits.');
assert(!personal.includes("digits(q('#personalRoomPasscode')?.value).slice"),'Packaged Personal Room editor must reject overlong passcodes instead of silently truncating them.');
assert(personal.includes('meeting.startPersonalRoom()'),'Packaged Personal Room Start must reopen the persistent room.');
assert(personal.includes('meeting.updatePersonalRoom'),'Packaged Settings must update Personal Room passcode/preferences server-side.');
assert(schedule.includes('Generate Automatically')&&schedule.includes('Personal Meeting ID'),'Packaged Schedule must expose Zoom-style Meeting ID choices.');
for(const repeat of ['Daily','Weekly','Monthly','Every weekday','Custom'])assert(schedule.includes(`>${repeat}<`),`Packaged Schedule missing recurrence option ${repeat}.`);
assert(schedule.includes('await meeting.schedule('),'Packaged Schedule must create a persistent scheduled identity rather than an instant room.');
assert(!schedule.includes('await meeting.create({title,passcode'),'Packaged Schedule must not create a fresh live room as its scheduling mechanism.');
assert(!schedule.includes("digits(q('#schedulePasscode')?.value).slice"),'Packaged Schedule must reject overlong passcodes instead of silently truncating them.');
assert(!schedule.includes("replace(/\\D/g,'').slice(0,11)"),'Packaged Schedule must not hide an invalid server Meeting ID by truncating it for display.');
assert(schedule.includes('await meeting.startSchedule(item.scheduleId)'),'Packaged recurring Start must reopen the existing scheduled identity.');
assert(schedule.includes("mode==='personal'&&recurrence"),'Packaged Schedule must prevent fixed recurrence from silently using the Personal Meeting ID.');
assert(scheduleCss.includes('.scheduled-row')&&scheduleCss.includes('.schedule-option-grid'),'Packaged scheduled/recurring meetings must have real desktop layouts.');
assert(av.includes("caret.className='av-device-caret attached-device-caret'")&&av.includes("button.classList.add('has-device-caret')"),'Packaged meeting must retain mic/video device-option carets attached to their controls.');
assert(av.includes("avGroup(detail,'Camera'")&&av.includes("avGroup(detail,'Appearance'")&&av.includes("avGroup(detail,'Background & Effects'")&&av.includes("summary.textContent='Advanced'"),'Packaged Video settings must preserve the approved Zoom-style hierarchy.');
assert(av.includes('Test Microphone')&&av.includes('Test Speaker')&&av.includes('Test Speaker & Microphone…'),'Packaged Audio settings must expose microphone/speaker tests from settings and the quick menu.');
assert(av.includes('Echo cancellation')&&av.includes('Noise suppression')&&av.includes('Automatically adjust microphone volume')&&av.includes('Original sound for musicians'),'Packaged Audio settings must expose real microphone processing controls.');
assert(media.includes("replaceKind('audio',state.microphoneId,true)"),'Packaged audio-processing changes must reacquire the live microphone with updated constraints.');
assert(parity.includes("version:'2.7.0-chat-share-dock-parity'"),'Packaged meeting must include the post-DOM Zoom-style meeting engine.');
assert(parity.includes("side.hidden=true;overlay.classList.add('participants-hidden')"),'Packaged participant management panel must be closed by default.');
assert(parity.includes("dock.dataset.orientation=(anchor==='top'||anchor==='bottom')?'horizontal':'vertical'"),'Packaged video dock must adapt orientation to position.');
assert(parity.includes("tile.classList.add('stage-promoted')"),'Packaged meeting must support active-speaker stage promotion.');
assert(parity.includes('desktop.meeting.context()')&&parity.includes('Passcode ${pass}'),'Packaged meeting must keep Meeting ID and passcode visible.');
assert(parity.includes("b.id='roomSettings'")&&parity.includes("q('#settingsDialog')")&&parity.includes('d.showModal()'),'Packaged Settings control must open the real Settings dialog.');
assert(parity.includes("b.id='roomMore'")&&parity.includes('openMore(b)'),'Packaged More control must open the real meeting menu.');
for(const action of ["add('Meeting settings'","add('Reset participant video panel'"])assert(parity.includes(action),`Packaged More menu is missing functional action: ${action}`);
assert(!parity.includes("add('Diagnostics'"),'Packaged production More menu must not expose QA Diagnostics.');
assert(parity.includes("window.addEventListener('dominion:meeting-ui-ready',()=>install())"),'Packaged Zoom parity must install after the live meeting DOM exists.');
assert(parity.includes("decorate(q('#roomMic'),SVG.mic)")&&parity.includes("decorate(q('#roomCamera'),SVG.video)")&&parity.includes("decorate(q('#roomShare'),SVG.share)")&&parity.includes("decorate(q('#roomParticipants'),SVG.participants)")&&parity.includes("decorate(q('#roomChat'),SVG.chat)")&&parity.includes("decorate(q('#roomReactions'),SVG.reactions)"),'Packaged primary controls must retain modern SVG icon decoration.');
assert(parity.includes("const desired=['roomMic','roomCamera','roomShare','roomParticipants','roomChat','roomReactions','roomMore','roomExitButton']"),'Packaged primary meeting toolbar order changed unexpectedly.');
assert(parityCss.includes('.meeting-body{position:relative!important;display:block!important'),'Packaged meeting must not reserve a permanent participant sidebar column.');
assert(parityCss.includes('.stage{position:absolute!important;inset:0!important'),'Packaged meeting stage must use the full canvas.');
assert(parityCss.includes('.room-side{position:absolute!important'),'Packaged participant management panel must overlay rather than shrink the stage.');
assert(parityCss.includes('.count-4 .participant-video-dock-body')&&parityCss.includes('grid-template-columns:repeat(2,176px)'),'Packaged participant video dock must grid at four to six tiles.');
assert(parityCss.includes('.count-7 .participant-video-dock-body')&&parityCss.includes('grid-template-columns:repeat(3,150px)'),'Packaged participant video dock must expand into a compact larger grid.');
assert(parityCss.includes('repeat(auto-fit,minmax(118px,1fr))'),'Packaged user-resized video dock must recompute its grid.');
for(const id of ['roomChat','roomRecord','roomReactions'])assert(features.includes(`'${id}'`),`Packaged meeting feature missing ${id}.`);
assert(features.includes("broadcast('chat',payload)")&&features.includes("broadcast('reaction',payload)"),'Packaged Chat and Reactions must use authenticated meeting signaling.');
assert(features.includes('new MediaRecorder('),'Packaged Record control must create a real local MediaRecorder.');
assert(features.includes('state.recorder.pause()')&&features.includes('state.recorder.resume()')&&features.includes('async function pauseRecording')&&features.includes('async function resumeRecording'),'Packaged computer recording must support Pause/Resume without starting a second recorder.');
assert(features.includes("stopRecording({reason:'revoked'})")&&features.includes('The host removed your recording permission.'),'Packaged recording must stop immediately and notify the participant when host permission is revoked.');
assert(features.includes("version:'1.4.0-zoom-chat-shell'")&&features.includes('recordingAuthority()')&&features.includes("['host','cohost'].includes(role)||Boolean(participant?.recordingAllowed)"),'Packaged recording must enforce Zoom-style host/co-host or host-granted participant authority.');
assert(features.includes('meetingRecordingConsent')&&features.includes('Leave Meeting')&&features.includes('Continue'),'Packaged participants must receive a clear recording notice with leave-or-continue choice.');
assert(features.includes('syncRemoteRecordingFromSnapshot')&&features.includes("broadcast('recording-state'"),'Packaged recording state must notify current participants and remain visible to late joiners.');
assert(features.includes("pref('recordMic')")&&features.includes("pref('recordRemote')"),'Packaged recording mixer must honor microphone and participant-audio preferences.');
assert(participantControls.includes("'Forbid Record':'Allow Record'")&&participantControls.includes('meeting.setRecordingPermission'),'Packaged host participant More menu must expose Allow Record / Forbid Record.');
assert(preload.includes('setRecordingPermission:')&&preload.includes('setRecordingState:')&&main.includes("'meeting:set-recording-permission'")&&main.includes("'meeting:set-recording-state'")&&meeting.includes('meet_v2_set_recording_permission')&&meeting.includes('meet_v2_set_recording_state'),'Packaged recording authority must cross only the narrow desktop bridge into backend RPC ownership.');
assert(read('ui','meeting-features.css').includes('.meeting-recording-consent')&&read('ui','meeting-features.css').includes('.recording-consent-orbit')&&read('ui','meeting-features.css').includes('.recording-participant-badge'),'Packaged recording consent and status must include dedicated futuristic desktop styling.');
assert(zoomBehavior.includes("version:'1.4.0'"),'Packaged app must include the stabilized Zoom behavior layer.');
assert(zoomBehavior.includes('Assign Host & Leave')&&zoomBehavior.includes('End Meeting for All'),'Packaged host exit must expose handoff versus end-for-all choices.');
assert(zoomBehavior.includes('showHostHandoffChoices')&&zoomBehavior.includes('meeting.transferHostAndLeave(participant.participantId)'),'Packaged host leave must use the real atomic handoff authority.');
assert(!zoomBehavior.includes("meeting.leave(ctx.participantId,ctx.joinToken)"),'Packaged host UI must not bypass the backend host-transfer safeguard.');
assert(preload.includes('transferHostAndLeave:')&&main.includes("'meeting:transfer-host-and-leave'")&&meeting.includes('meet_v2_transfer_host_and_leave'),'Packaged desktop must wire host handoff across renderer bridge, main IPC, and meeting service.');
assert(zoomBehavior.includes("button.id='zoomAdmitAll'")&&zoomBehavior.includes("meeting.decide(p.participantId,'admit')"),'Packaged waiting room must include Admit All.');
assert(participantControls.includes("add('Mute'")&&participantControls.includes("add('Ask to Unmute'")&&participantControls.includes("add('Stop Video'")&&participantControls.includes("add('Ask to Start Video'"),'Packaged participant More menu must expose host/co-host media controls.');
assert(participantControls.includes('Stop Video for All')&&participantControls.includes('Ask All to Start Video'),'Packaged participant panel must expose meeting-wide video controls.');
assert(participantControls.includes("meeting.renameParticipant(id,name)")&&preload.includes('renameParticipant:')&&main.includes("'meeting:rename-participant'")&&meeting.includes('meet_v2_rename_participant'),'Packaged participant rename must be wired through renderer bridge, IPC, and backend authority.');
assert(participantControls.includes("if(localRole()==='host'&&role!=='cohost')add('Make Co-host'"),'Packaged co-host promotion control must remain host-only.');
assert(participantControls.includes('authorizedSender')&&participantControls.includes("['host','cohost'].includes(String(sender?.role||'').toLowerCase())"),'Packaged remote media controls must verify host/co-host sender authority.');
assert(participantControls.includes('requestConsent')&&participantControls.includes("type==='host:ask-unmute'"),'Packaged Ask to Unmute must preserve participant consent.');
assert(features.includes("kind:'hand'")&&features.includes('toggleRaiseHand')&&features.includes('raisedHands:new Map()'),'Packaged Reactions must include persistent Raise Hand state.');
assert(features.includes('raised-hand-indicator')&&features.includes('remote-raised-hand'),'Packaged Raise Hand must decorate roster and participant video.');
assert(features.includes("const reactions=['👏','👍','❤️','😂','😮','🎉']"),'Packaged reactions must use Zoom’s standard six reaction set.');
assert(features.includes('setParticipantReaction')&&features.includes('},10000))'),'Packaged ordinary reactions must remain attached to participant identity for 10 seconds.');
assert(features.includes('participant-reaction-indicator')&&features.includes('remote-reaction-indicator'),'Packaged emoji reactions must appear on roster and participant video identity surfaces.');
assert(presenterJs.includes("['👏','👍','❤️','😂','😮','🎉']"),'Packaged presenter toolbar must use the same Zoom-standard reaction set.');
assert(presenterHtml.includes('presenterRecordCommand')&&presenterHtml.includes('presenterStopRecord')&&presenterJs.includes("state?.recordingPaused?'Resume recording':'Pause recording'")&&read('ui','share-integration.js').includes("command==='record'")&&read('ui','share-integration.js').includes("command==='stop-record'"),'Packaged presenter toolbar must preserve Record/Pause/Resume/Stop controls while screen sharing.');
assert(html.includes('<script src="./meeting-captions.js"></script>'),'Packaged meeting shell must load the captions/transcript module.');
assert(captions.includes("version:'1.3.0'")&&captions.includes("button.id='roomCaptions'")&&captions.includes("caret.id='roomCaptionsMenu'"),'Packaged toolbar must expose Show/Hide Captions and caption options.');
assert(captions.includes('const cutoff=now()-180000')&&captions.includes('Past 3 minutes'),'Packaged live captions must keep only a three-minute accessibility window.');
assert(captions.includes("add('I will type'")&&captions.includes('Assign participant to type')&&captions.includes("add('Assign participant'")&&captions.includes('meeting.setCaptionState')&&captions.includes('meeting.publishCaption')&&captions.includes("const canManage=()=>state.role==='host'"),'Packaged manual captioning must match Zoom host-only self-type and assign-participant flows through server-backed authority.');
assert(captions.includes("add('Request Captions'")&&captions.includes("meeting.sendSignal(p.participantId,'caption-request',payload)")&&captions.includes('requested captions.'),'Packaged non-host users must be able to request captions from the host.');
assert(captions.includes('Retain Meeting Transcript')&&captions.includes('meeting.transcript(ctx.roomId)'),'Packaged transcript retention must remain an explicit separate host control.');
assert(captions.includes('Automated captions are not enabled in this QA build until a stable speech engine is certified.'),'Packaged desktop must not fake automated caption support.');
assert(captionsCss.includes('.meeting-caption-overlay')&&captionsCss.includes('.meeting-caption-panel')&&captionsCss.includes('.meeting-caption-menu'),'Packaged captions must include dedicated accessibility styling.');
assert(preferences.includes('Closed Captioning')&&preferences.includes('captionFontSize')&&preferences.includes('captionFontType')&&preferences.includes('captionTheme')&&preferences.includes('captionPosition')&&preferences.includes('alwaysShowCaptions'),'Packaged Accessibility settings must expose Zoom-style caption appearance preferences.');
assert(captions.includes("pref('captionFontSize'")&&captions.includes("pref('captionFontType'")&&captions.includes("pref('captionTheme'")&&captions.includes("pref('captionPosition'"),'Packaged caption renderer must apply personal appearance settings.');
assert(captions.includes("overlay.classList.toggle('caption-popout'")&&captionsCss.includes('.meeting-caption-overlay.caption-popout')&&captionsCss.includes('resize:both'),'Packaged captions must support pinned-bottom and movable/resizable overlay positions.');
assert(captions.includes("window.addEventListener('dominion:preference-change'"),'Packaged caption appearance must update live when preferences change.');
assert(!preferences.includes('setCaptionState(')&&!preferences.includes('publishCaption('),'Packaged personal Accessibility settings must remain isolated from host caption/transcript authority.');
assert(preload.includes('setCaptionState:')&&preload.includes('publishCaption:')&&preload.includes('transcript:')&&main.includes("'meeting:set-caption-state'")&&main.includes("'meeting:publish-caption'")&&main.includes("'meeting:get-transcript'"),'Packaged caption and transcript authority must cross only the narrow desktop IPC bridge.');
assert(meeting.includes('meet_v2_set_caption_state')&&meeting.includes('meet_v2_publish_caption')&&meeting.includes('meet_v2_get_transcript'),'Packaged meeting service must own caption/transcript RPC access.');
assert(participantControls.includes("type==='host:lower-hand'")&&participantControls.includes("row.dataset.raisedHand==='1'")&&participantControls.includes('Lower All Hands'),'Packaged host/co-host controls must support Lower Hand and Lower All Hands through verified authority.');
assert(presenterJs.includes("'toggle-hand'")&&read('ui','share-integration.js').includes("command==='toggle-hand'"),'Packaged presenter toolbar must let a presenter raise/lower their hand without reopening the full meeting window.');
assert(participantControls.includes('Spotlight for Everyone')&&parity.includes('spotlightParticipantId'),'Packaged Spotlight must drive the meeting stage authority.');
assert(!read('ui','app.js').includes('data-cohost='),'Packaged roster must not retain duplicate inline participant authority buttons.');
assert(parity.includes('Lock Meeting')&&parity.includes('Mute Participants on Entry')&&preload.includes('setSecurity:')&&main.includes("'meeting:set-security'")&&meeting.includes('meet_v2_set_security'),'Packaged Security menu must use server-backed Lock Meeting and Mute-on-Entry authority.');
assert(parity.includes("button.id='meetingViewButton'")&&parity.includes("['speaker','gallery','multi']"),'Packaged meeting must expose Speaker, Gallery, and Multi-speaker views.');
assert(parity.includes("return stage.width<900||stage.height<560?'top':'right'")&&parity.includes("user-positioned"),'Packaged participant dock must auto-reflow for compact windows without overriding manual placement.');
assert(parity.includes("const floatingPanel=active&&showPanel&&!sideBySide")&&parity.includes("DominionPreferences?.read?.('shareSideBySide')===true"),'Packaged screen sharing must default to the movable floating video panel while preserving optional side-by-side mode.');
assert(features.includes('meetingChatRecipient')&&features.includes('In-meeting messages'),'Packaged Chat must render a polished recipient-aware in-meeting panel.');

assert(parityCss.includes('.participant-video-dock.gallery-stage')&&parityCss.includes('.participant-video-dock.multi-speaker-stage')&&parityCss.includes('.active-speaker-rank-4'),'Packaged adaptive view styling must include Gallery and up-to-four-speaker Multi-speaker layouts.');
assert(webrtc.includes("ranked.slice(0,4)")&&webrtc.includes("new CustomEvent('dominion:active-speakers'"),'Packaged WebRTC must rank and publish up to four active speakers for layout intelligence.');
assert(parity.includes("sharing()?'Side-by-side: Speaker':'Speaker'")&&parity.includes("sharing()?'Side-by-side: Gallery':'Gallery'")&&parity.includes("sharing()?'Side-by-side: Multi-speaker':'Multi-speaker'"),'Packaged View menu must expose Zoom-style side-by-side share layouts.');
assert(parity.includes("splitter.setAttribute('role','separator')")&&parity.includes("saveShareSplit(ratio)"),'Packaged screen sharing must provide an adjustable persistent share/video divider.');
assert(parity.includes("panel.textContent=visible?'Hide Video Panel':'Show Video Panel'")&&parity.includes("DominionPreferences?.write?.('shareVideoDock'"),'Packaged share View menu must provide Hide/Show Video Panel using the existing preference authority.');
assert(parity.includes("featured?.classList.add('share-featured')"),'Packaged side-by-side Speaker view must emphasize one current speaker/spotlight.');
assert(parityCss.includes('.share-layout-splitter')&&parityCss.includes('.meeting-overlay.share-side-by-side[data-share-view="gallery"]')&&parityCss.includes('.meeting-overlay.share-side-by-side[data-share-view="multi"]'),'Packaged meeting must include side-by-side Speaker/Gallery/Multi-speaker styling.');
assert(zoomBehavior.includes("select.id='meetingChatRecipient'")&&zoomBehavior.includes("meeting.sendSignal(target,'chat',payload)"),'Packaged chat must support direct private recipients.');
assert(zoomBehavior.includes('meeting-chat-unread-badge')&&zoomBehavior.includes('unreadCount+=1')&&zoomBehavior.includes('updateUnread(true)'),'Packaged chat must retain a persistent unread count until opened.');
assert(zoomBehavior.includes("select.id='meetingChatPolicy'")&&zoomBehavior.includes('meeting.setChatPolicy(ctx.roomId,select.value)')&&preload.includes('setChatPolicy:')&&main.includes("'meeting:set-chat-policy'")&&meeting.includes('meet_v2_set_chat_policy'),'Packaged host/co-host chat policy must be wired across renderer, IPC, and backend authority.');
assert(zoomBehavior.includes("policy==='host_cohost'")&&zoomBehavior.includes('Chat disabled by host'),'Packaged participant chat recipient choices must follow the meeting chat policy.');
assert(zoomBehavior.includes("DominionMeetingNotifications?.chat?.(name||'Participant')"),'Packaged Zoom-style incoming chat must honor the shared notification authority.');
assert(zoomBehavior.includes("select.dataset.recipientSignature!==signature"),'Packaged private-chat recipient sync must avoid redundant DOM rewrites.');
assert(!zoomBehavior.includes('new MutationObserver('),'Packaged Zoom behavior must not install a DOM-wide mutation observer.');
assert(zoomBehavior.includes('const syncTimer=setInterval(sync,900)')&&zoomBehavior.includes('if(!isMeetingOpen())return;'),'Packaged Zoom behavior synchronization must be bounded and meeting-state driven.');
assert(zoomBehavior.includes("input.maxLength=7")&&zoomBehavior.includes("input.pattern='[0-9]{3,7}'"),'Packaged new/join passcode controls must match the 3–7 digit authority.');
assert(zoomCss.includes('.zoom-leave-dialog')&&zoomCss.includes('.zoom-admit-all')&&zoomCss.includes('.meeting-chat-recipient'),'Packaged Zoom behavior styling must be present.');
assert(participantControls.includes("type==='host:mute'")&&participantControls.includes("type==='host:stop-video'"),'Packaged participant controls must support verified forced mute and stop-video.');
assert(participantControls.includes("type==='host:ask-unmute'")&&participantControls.includes("type==='host:ask-start-video'"),'Packaged participant controls must require consent for unmute and start-video requests.');
assert(participantControls.includes('authorizedSender')&&participantControls.includes("['host','cohost'].includes"),'Packaged participant controls must verify host/co-host authority before executing media commands.');
assert(participantControls.includes('Mute All')&&participantControls.includes('Ask All to Unmute'),'Packaged participant panel must expose bulk Zoom-style audio controls.');
assert(participantControls.includes("filter(p=>String(p.role||'').toLowerCase()!=='host')"),'Packaged bulk media controls must exclude the host.');
assert(webrtc.includes("String(signal.type||'').startsWith('host:')"),'Packaged WebRTC must dispatch host-control signals to the participant-control layer.');
assert(participantCss.includes('.participant-control-menu')&&participantCss.includes('.participant-control-prompt'),'Packaged participant controls must include dedicated desktop styling.');
for(const key of ['joinMuted','joinVideoOff','showJoinPreview','hideSelfView','shareVideoDock','shareOptimize','shareAudio','chatSound','recordMic','recordRemote','uiScale','shortcuts'])assert(preferences.includes(`${key}:`),`Packaged preferences missing ${key}.`);
assert(preferences.includes('Show video preview before joining')&&preferences.includes('Hide my self view in meetings'),'Packaged meeting settings must expose join-preview and self-view controls.');
assert(read('ui','app.js').includes('joinUsingSavedDefaults')&&read('ui','app.js').includes("DominionPreferences?.read?.('showJoinPreview')===false"),'Packaged app must skip preview only through saved defaults and the normal meeting flow.');
assert(parity.includes("DominionPreferences?.read?.('hideSelfView')"),'Packaged participant dock must honor Hide Self View without stopping the camera.');
assert(annotation.includes('setAnnotationCanvas')&&annotation.includes('share-annotation-canvas'),'Packaged annotation UI must attach a real drawing canvas to the share controller.');
assert(annotation.includes("data-annotation-mode=\"laser\"")&&annotation.includes('drawLaser')&&annotation.includes('clearLaser(650)'),'Packaged annotation must include a transient laser pointer.');
assert(annotation.includes('data-annotation-undo')&&annotation.includes('state.history.pop()'),'Packaged annotation must include Undo.');
for(const color of ['#ff3b30','#2d8cff','#28c76f','#ffffff'])assert(annotation.includes(`data-annotation-color="${color}"`),`Packaged annotation palette missing ${color}.`);
assert(shareController.includes('state.paused=true;if(state.annotationCanvas)startComposite()')&&shareController.includes('state.paused=false;if(state.annotationCanvas)startComposite()'),'Packaged annotations must remain composited across Pause and Resume transitions.');
assert(shareController.includes('canvas.captureStream(30)'),'Packaged share controller must create an outgoing composite stream for annotations.');
assert(shareController.includes('ctx.drawImage(state.annotationCanvas'),'Packaged share controller must draw annotations into the outgoing shared frame.');
assert(shareController.includes('state.annotationCanvas&&state.compositeStream?state.compositeStream:baseOutputStream()'),'Packaged outgoing share must switch to annotation composite only while annotations are active.');
assert(shareController.includes("presenter?.toolbarReady===false"),'Packaged capture must fail closed when presenter controls cannot start.');
assert(shareController.includes("Presenter controls could not start. Screen sharing was cancelled safely."),'Packaged toolbar-start failure must visibly report safe cancellation.');
assert(shareController.includes('async function replaceSource')&&shareController.includes('const previousLive=state.liveStream'),'Packaged New Share must use a transactional source-replacement path.');
assert(shareController.indexOf('const {stream,track}=await acquireDisplay(options);',shareController.indexOf('async function replaceSource'))<shareController.indexOf('stopTracks(previousFrozen);stopTracks(previousLive);',shareController.indexOf('async function replaceSource')),'Packaged New Share must acquire the replacement before stopping the old presentation.');
assert(read('ui','share-integration.js').includes("if(command==='new-share'){await openPickerWithPermission();return;}")&&read('ui','share-integration.js').includes('if(replacing){await share.replaceSource'),'Packaged presenter New Share must keep the old presentation live through picker selection.');
assert(shareService.includes('if(toolbarWindow&&!toolbarWindow.isDestroyed())')&&shareService.includes('publishToolbarState();')&&shareService.includes('return true;'),'Packaged presenter toolbar must reuse one existing toolbar instance across source changes.');
assert(!shareService.slice(shareService.indexOf("ipcMain.handle('share:select-source'"),shareService.indexOf("ipcMain.handle('share:capture-stopped'")).includes('closeToolbar()'),'Packaged New Share/source selection must not close the presenter toolbar.');
assert(presenterHtml.includes('shareSourceLabel')&&presenterHtml.includes('shareAudioFlag')&&presenterHtml.includes('shareOptimizeFlag'),'Packaged presenter toolbar must show source and active Computer Sound / Optimize states.');
assert(presenterJs.includes('state?.shareAudio')&&presenterJs.includes('state?.optimizeVideo'),'Packaged presenter toolbar flags must follow live share state.');
assert(read('ui','share-integration.js').includes('shareAudio:Boolean(state.options?.shareAudio)')&&read('ui','share-integration.js').includes('optimizeVideo:Boolean(state.options?.optimizeVideo)'),'Packaged presenter state must come from real share options.');
assert(webrtc.includes('remoteShareBanner')&&webrtc.includes('is sharing')&&!webrtc.includes('Share paused'),'Packaged recipients must see who is sharing without presenter-only Pause state.');
assert(read('ui','webrtc.css').includes('.remote-share-banner'),'Packaged remote-share identity must have viewer styling.');
assert(read('ui','share.css').includes('.meeting-overlay.share-active .meeting-footer{display:none}'),'Packaged active share must suppress the ordinary meeting footer so the presenter toolbar is the share-control surface.');
assert(share.includes('let inFlight=null'),'Packaged share authority must keep exactly one native source enumeration in flight.');
assert(share.includes('Promise.race([inFlight,timeoutResult()])'),'Packaged share source enumeration must remain bounded by timeout.');
assert(share.includes("if(inFlight===tracked){inFlight=null;inFlightKey='';}"),'Packaged share authority must release only the completing active single-flight enumeration.');
assert(shareService.includes('createShareSourceAuthority'),'Packaged share service must use the isolated source authority.');
assert.equal((read('ui','media-controller.js').match(/script\.src='\.\/share-integration\.js'/g)||[]).length,1,'Packaged media controller must own exactly one native Share integration bootstrap path.');
assert(!read('ui','index.html').includes('<script src="./share-integration.js"></script>'),'Packaged desktop HTML must not install a competing static Share integration loader.');
assert(read('ui','share-integration.js').includes("button.id='roomShare'"),'Packaged Share integration must create the in-meeting Share Screen control.');
assert(shareService.includes('ensureScreenPermission()')&&shareService.includes('permissionRequired:true'),'Packaged share picker must preflight native Screen Recording permission.');
assert(webrtc.includes('RTCPeerConnection'),'Packaged app must include WebRTC transport.');
assert((webrtc.match(/addTransceiver\('audio'/g)||[]).length===2 && (webrtc.match(/addTransceiver\('video'/g)||[]).length===2,'Packaged peer transport must contain microphone, camera, screen-video, and shared-system-audio lanes.');
assert(webrtc.includes('replaceTrack(shareAudio)')&&webrtc.includes("if(index===3&&event.track.kind==='audio')")&&webrtc.includes('playRemoteShareAudio'),'Packaged Share Computer Sound must travel on its own WebRTC lane to remote participants.');
assert(webrtc.includes("degradationPreference=shareState.options?.optimizeVideo?'maintain-framerate':'balanced'")&&webrtc.includes('4500000:2500000'),'Packaged Optimize for Video must tune the outgoing screen sender.');
assert(shareController.includes("track.contentHint=optimize?'motion':'detail'")&&shareController.includes("audioTrack.contentHint='music'"),'Packaged share capture must set appropriate screen and system-audio content hints.');
assert(shareController.includes('frozen.addTrack(audioTrack.clone())'),'Packaged Pause Share must preserve presentation audio while freezing the visual frame.');
assert(webrtc.includes('meeting.iceConfig'),'Packaged app must include relay-capable ICE configuration.');
assert(webrtc.includes("window.addEventListener('offline',handleOffline)")&&webrtc.includes("window.addEventListener('online',handleOnline)")&&webrtc.includes('async function recoverNetwork()'),'Packaged desktop must preserve the live meeting and recover transport across network interruptions.');
assert(webrtc.includes('await loadIceConfig(true)')&&webrtc.includes('pc.restartIce()')&&webrtc.includes('await syncAllSenders()'),'Packaged reconnect must refresh relay credentials, restart ICE, and resync media without rejoining.');
assert(!webrtc.includes('meeting.requestJoin(')&&!webrtc.includes('meeting.markJoined('),'Packaged WebRTC recovery must not replay meeting admission.');
assert(read('ui','webrtc.css').includes('.network-recovery-banner'),'Packaged reconnect state must have dedicated desktop styling.');
assert(webrtc.includes('await effects.outputStream(raw)')&&webrtc.includes('state.effectsUnsub=window.DominionVideoEffects?.onChange'),'Packaged WebRTC must send the processed camera track when video effects are enabled.');
fs.rmSync(unpackDir,{recursive:true,force:true});
console.log('DOMINIONSTAR_PACKAGED_APP_CERTIFIED personal-room persistent-passcode exact-input-validation recurring-identity generated-11-digit schedule no-dead-home-chrome real-brand auth media zoom-stage zoom-behavior atomic-host-handoff participant-authority bulk-controls mute ask-unmute stop-video raise-hand lower-hand lower-all-hands zoom-standard-reactions reaction-10s identity-reactions captions manual-captioner transcript-separation caption-accessibility caption-popout request-captions host-only-caption-setup manual-captioner-choice recording-authority recording-consent late-join-recording recording-pause-resume revoke-stop presenter-recording-controls futuristic-recording-ui rename spotlight host-only-cohost security-lock mute-on-entry waiting-room-alert join-leave-alert native-background-notification app-badge background-attention admit-all private-chat unread-chat chat-policy participant-media-controls bounded-sync auto-framing background-blur virtual-backgrounds feathered-foreground adaptive-performance video-denoise processed-camera share transactional-new-share system-audio optimize-video presenter-state remote-share-identity single-presenter-toolbar annotation-laser annotation-undo annotation-colors paused-annotation webrtc reconnect-recovery sleep-wake-recovery presence-heartbeat ghost-peer-pruning adaptive-views gallery multi-speaker responsive-dock side-by-side-share draggable-divider hide-video-panel host-view-broadcast spotlight-side-by-side diagnostics');
