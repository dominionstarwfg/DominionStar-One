import fs from 'node:fs';
import assert from 'node:assert/strict';

const read=rel=>fs.readFileSync(new URL(`../${rel}`,import.meta.url),'utf8');
const main=read('src/main.mjs');
const preload=read('src/preload.cjs');
const html=read('ui/index.html');
const css=read('ui/styles.css');
const js=read('ui/app.js');
const preferences=read('ui/preferences.js');
const avSettings=read('ui/av-settings.js');
const personal=read('ui/personal-room.js');
const personalCss=read('ui/personal-room.css');
const schedule=read('ui/schedule-controller.js');
const scheduleCss=read('ui/schedule.css');
const shareService=read('src/share-service.mjs');
const shareCss=read('ui/share.css');
const presenterHtml=read('ui/presenter-toolbar.html');
const presenterCss=read('ui/presenter-toolbar.css');
const presenterJs=read('ui/presenter-toolbar.js');
const packageJson=JSON.parse(read('package.json'));
const appIcon=read('build/icon.svg');
const meetingNotifications=read('ui/meeting-notifications.js');
const meetingNotificationsCss=read('ui/meeting-notifications.css');
const meetingCaptions=read('ui/meeting-captions.js');
const meetingCaptionsCss=read('ui/meeting-captions.css');
const meetingFeatures=read('ui/meeting-features.js');
const meetingFeaturesCss=read('ui/meeting-features.css');

assert(main.includes("const uiDir=path.join(__dirname,'..','ui')"),'Desktop must define one local UI directory authority.');
assert(main.includes("mainWindow.loadFile(path.join(uiDir,'index.html'))"),'Desktop must load Home from the local UI directory.');
assert(!main.includes('dominionstarld.com'),'Desktop shell must not depend on the public website.');
assert(main.includes("if(url.startsWith('file://'))return"),'Navigation must remain local by default.');
assert(preload.includes('contextIsolation')===false,'Preload should expose only the explicit bridge, not runtime configuration.');
for(const label of ['New Meeting','Join','Schedule','Share Screen'])assert(html.includes(`>${label}<`),`Missing Home action: ${label}`);
for(const section of ['homeSection','meetingsSection'])assert(html.includes(`id=\"${section}\"`),`Missing desktop section ${section}`);
assert(!html.includes('id="contactsSection"')&&!html.includes('data-section="contacts"'),'Dead Contacts placeholder must not ship.');
assert(!html.includes('aria-label="Search"'),'Dead Search control must not ship.');
assert(!html.includes('will live here')&&!html.includes('wired after'),'Developer placeholder copy must not ship.');
for(const script of ['./meeting-captions.js','./meeting-notifications.js','./preferences.js','./personal-room.js','./schedule-controller.js'])assert(html.includes(`<script src=\"${script}\"></script>`),`Desktop must load ${script}.`);
for(const style of ['./personal-room.css','./schedule.css'])assert(html.includes(`<link rel=\"stylesheet\" href=\"${style}\">`),`Desktop must load ${style}.`);
assert(html.includes('id="scheduleForm"')&&html.includes('id="scheduledMeetingList"'),'Schedule and Meetings must have real UI surfaces.');
assert(!html.includes('meet_personal_rooms'),'Undeployed legacy backend table name must never leak into the Home UI.');
assert(css.includes('.action-card:active{transform:translateY(0) scale(.99)}'),'Action controls must visibly release after click.');
assert(js.includes("document.body.dataset.shareAfterJoin='1';openDialog('join')"),'Home Share must enter the real meeting/share flow.');
assert(!js.includes("notice('Share remains isolated'"),'Home Share must not return to the old placeholder notice.');
assert(!js.includes('getDisplayMedia'),'Home/room controller must not own screen capture.');
assert(!/(?<!\\$)\\$\\([^\\n;]{0,180}\\)\\.forEach/.test(js),'Home bootstrap must never call forEach on the single-element $ selector; multi-element bindings must use $.');
assert(js.includes("greeting=hour<12?'Good morning':hour<17?'Good afternoon':'Good evening'"),'Home greeting must be time-aware.');
assert(js.includes("new CustomEvent('dominion:meeting-ui-ready')"),'Base meeting UI must announce when the live DOM exists so parity modules cannot race startup.');
assert(js.includes("room.className='meeting-overlay participants-hidden'")&&js.includes('<aside class="room-side" hidden>'),'Base meeting shell must never flash the Participants/Waiting Room panel open.');
assert(js.includes("profileStatus.textContent=rank||'Available'"),'Home account chip must use resolved member rank when available.');
assert(main.includes('app.isInApplicationsFolder()'),'Packaged Mac app must expose stable /Applications installation state.');
assert(js.includes("info?.installedInApplications===false")&&js.includes('Install DominionStar Meet first'),'Renderer must warn when a packaged Mac copy is run outside /Applications so TCC permissions bind to the installed app.');
assert(html.includes('id="changeProfilePicture"')&&html.includes('id="profileAvatarInput"'),'Profile dialog must expose profile-picture editing.');
assert(js.includes("auth.updateAvatar(dataUrl)")&&js.includes("setAvatar($('#profileAvatar')"),'Desktop Home must upload and render the signed-in member avatar.');
assert(js.includes("classList.toggle('is-off',!s.micOn)")&&js.includes("classList.toggle('is-off',!s.cameraOn)"),'Prejoin and meeting controls must expose mic/video off state.');
assert(js.includes("DominionMeetingNotifications?.play?.(media.snapshot().micOn?'mic-on':'mic-off')"),'Local microphone toggles must produce state feedback sound.');

assert(personal.includes('Use Personal Meeting ID'),'New Meeting must expose the Personal Meeting ID choice.');
assert(personal.includes('useForInstant'),'Personal Meeting ID instant-meeting preference must be respected.');
assert(personal.includes("toggle.checked=Boolean(state.room&&state.room.useForInstant!==false)"),'New Meeting must never select Personal Meeting ID unless a Personal Room actually loaded.');
assert(personal.includes("if(!toggle?.checked||!state.room)return"),'Unavailable Personal Room must fall through to a normal instant meeting instead of blocking New Meeting.');
assert(personal.includes('meeting.startPersonalRoom()'),'Personal Room Start must reopen the persistent Personal Room identity.');
assert(personal.includes('meeting.updatePersonalRoom'),'Personal Room settings must persist through the meeting authority.');
assert(personal.includes('pattern="[0-9]{3,7}"')&&personal.includes('maxlength="7"'),'Personal Room passcode UI must accept 3–7 digits only.');
assert(personal.includes('Changing the passcode does not change the Personal Meeting ID.'),'Personal Room editor must communicate stable Meeting ID behavior.');
assert(personalCss.includes('.personal-room-card')&&personalCss.includes('.personal-room-modal'),'Personal Room must have a real Meetings card and editor layout.');

assert(schedule.includes('Generate Automatically')&&schedule.includes('Personal Meeting ID'),'Schedule must offer generated identity or Personal Meeting ID.');
for(const repeat of ['Daily','Weekly','Monthly','Every weekday','Custom'])assert(schedule.includes(`>${repeat}<`),`Schedule recurrence option missing ${repeat}.`);
assert(schedule.includes('await meeting.schedule('),'Schedule must create a persistent scheduled identity through the meeting authority.');
assert(!schedule.includes('await meeting.create({title,passcode'),'Schedule must not create a fresh live room as its scheduling mechanism.');
assert(schedule.includes('await meeting.startSchedule(item.scheduleId)'),'Starting a scheduled/recurring meeting must reopen the existing meeting identity.');
assert(schedule.includes("mode==='personal'&&recurrence"),'Fixed recurrence must not silently reuse the Personal Meeting ID.');
assert(schedule.includes("/^\\d{3,7}$/"),'Generated scheduled meeting passcodes must be validated as 3–7 digits.');
assert(scheduleCss.includes('.scheduled-row')&&scheduleCss.includes('.scheduled-home-list')&&scheduleCss.includes('.schedule-option-grid'),'Scheduled and recurring meetings must render on Home, Meetings, and Schedule surfaces.');

for(const key of ['joinMuted','joinVideoOff','showJoinPreview','hideSelfView','waitingRoomSound','joinLeaveSound','desktopMeetingNotifications','shareVideoDock','shareOptimize','shareAudio','chatSound','recordMic','recordRemote','uiScale','shortcuts','captionFontSize','captionFontType','captionTheme','captionPosition','alwaysShowCaptions'])assert(preferences.includes(`${key}:`),`Preferences missing ${key}.`);
for(const section of ['Meetings','Share Screen','Chat','Recording','Accessibility','Keyboard Shortcuts','About'])assert(preferences.includes(`'${section}'`),`Preferences section missing: ${section}`);
assert(!preferences.includes('will be added'),'Preferences must not expose dead future-feature copy.');
assert(!avSettings.includes("else if(title==='Meetings')")&&!avSettings.includes("else if(title==='Sharing')"),'A/V settings must not own or overwrite Meetings/Sharing preference actions.');
assert(!avSettings.includes('core meeting path is physically approved')&&!avSettings.includes('Share defaults will be exposed here'),'Decorative settings placeholder copy must not ship.');
assert(preferences.includes("dialog.querySelector('.settings-list').hidden=true")&&preferences.includes("form.scrollTop=0"),'Settings category clicks must replace the list with actionable controls at the top of the modal.');
assert(preferences.includes('Show video preview before joining')&&preferences.includes('Hide my self view in meetings'),'Meeting preferences must expose Zoom-style join-preview and self-view choices.');
assert(js.includes("DominionPreferences?.read?.('showJoinPreview')===false")&&js.includes('joinUsingSavedDefaults'),'Skipping preview must still enter through the saved media defaults and normal meeting authority.');
assert(js.includes("DominionPreferences?.read?.('hideSelfView')")&&js.includes("$('#localMeetingVideo').hidden=!s.videoLive||hideSelf"),'Hide Self View must affect only the local meeting video surface.');
assert(preferences.includes('Play a sound when someone enters the Waiting Room')&&preferences.includes('Play a sound when participants join or leave')&&preferences.includes('Show desktop meeting notifications'),'Meeting preferences must expose Zoom-style waiting-room and join/leave notification controls.');
assert(js.includes("new CustomEvent('dominion:waiting-room-update'")&&js.includes("new CustomEvent('dominion:participant-presence'"),'Existing room polling must emit notification events instead of adding another backend poller.');
assert(meetingNotifications.includes("version:'1.1.0'")&&meetingNotifications.includes("participantBadge(items.length)")&&meetingNotifications.includes("play('waiting')")&&meetingNotifications.includes("play('join')")&&meetingNotifications.includes("play('leave')"),'Meeting notification layer must provide waiting-room badge and sound cues.');
assert(meetingNotifications.includes("ctx?.setSinkId&&speakerId")&&meetingNotifications.includes("DominionMediaController?.snapshot?.().speakerId"),'Meeting alert tones must follow the selected speaker when the runtime supports audio-output routing.');
assert(meetingNotifications.includes("if(kind==='chat'&&!pref('chatSound',true))return")&&meetingNotifications.includes("function chat(name='Participant')"),'Meeting notification authority must honor the Chat sound preference.');
assert(read('ui/meeting-features.js').includes("DominionMeetingNotifications?.chat?.(message.name||'Participant')"),'Incoming hidden-panel chat messages must use the shared notification authority.');
assert(meetingNotifications.includes("if(!pref('desktopMeetingNotifications',true)||document.hasFocus())return"),'Native meeting notifications must be reserved for background/unfocused app state.');
assert(meetingNotificationsCss.includes('.waiting-room-badge')&&meetingNotificationsCss.includes('.meeting-event-toast'),'Meeting notification visuals must ship with dedicated styling.');
assert(meetingFeatures.includes("version:'1.3.0'")&&meetingFeatures.includes('recordingAuthority()'),'Recording must have a dedicated permission check before creating a local recording.');
assert(meetingFeatures.includes("['host','cohost'].includes(role)||Boolean(participant?.recordingAllowed)"),'Recording authority must allow host/co-host by role and ordinary participants only through explicit host permission.');
assert(meetingFeatures.includes("meeting.setRecordingState(authority.ctx.participantId,true,false)")&&meetingFeatures.includes("meeting.setRecordingState(authority.ctx.participantId,false,false)"),'Recording start/stop must persist authoritative participant recording state.');
assert(meetingFeatures.includes('state.recorder.pause()')&&meetingFeatures.includes('state.recorder.resume()')&&(meetingFeatures.match(/new MediaRecorder\(/g)||[]).length===1,'Recording must pause/resume the same MediaRecorder rather than creating a second recording file.');
assert(meetingFeatures.includes('async function pauseRecording')&&meetingFeatures.includes('async function resumeRecording')&&meetingFeatures.includes("announceRecording(true,true)")&&meetingFeatures.includes("announceRecording(true,false)"),'Pause/Resume must persist and broadcast recording pause state.');
assert(meetingFeatures.includes("if(state.recording&&!allowed){void stopRecording({reason:'revoked'});return;}"),'Revoking participant recording permission must stop an active local recorder.');
assert(meetingFeatures.includes('The host allowed you to record this meeting.')&&meetingFeatures.includes('The host removed your recording permission.'),'Participants must be notified when recording permission is granted or revoked.');
assert(meetingFeatures.includes("broadcast('recording-state'")&&meetingFeatures.includes('syncRemoteRecordingFromSnapshot'),'Recording state must notify current participants and remain discoverable to late joiners.');
assert(meetingFeatures.includes('meetingRecordingConsent')&&meetingFeatures.includes('Leave Meeting')&&meetingFeatures.includes('Continue'),'Participants must receive a recording notice with a clear leave-or-continue choice.');
assert(meetingFeatures.includes("pref('recordMic')")&&meetingFeatures.includes("pref('recordRemote')"),'Recording audio preferences must control the actual recording mixer.');
assert(meetingFeatures.includes('await meeting.setRecordingState(authority.ctx.participantId,false,false)')&&meetingFeatures.includes("if(recorder.state!=='inactive')recorder.stop()"),'Failed recording startup must roll back server state and stop recorder resources.');
assert(meetingFeaturesCss.includes('.meeting-recording-consent')&&meetingFeaturesCss.includes('.recording-consent-orbit')&&meetingFeaturesCss.includes('.recording-participant-badge'),'Recording notice, status, and participant badges must ship with dedicated futuristic desktop styling.');
assert(main.includes('Notification')&&main.includes("ipcMain.handle('notifications:meeting'")&&main.includes('new Notification('),'Native shell must own desktop meeting notifications.');
assert(main.includes("ipcMain.handle('notifications:set-waiting-count'")&&main.includes("app.dock.setBadge")&&main.includes("app.dock.bounce('informational')")&&main.includes("mainWindow.flashFrame"),'Background Waiting Room alerts must use native app badge and attention behavior.');
assert(meetingNotifications.includes("desktop.notifications?.setWaitingCount?.(items.length,attention)")&&meetingNotifications.includes("desktop.notifications?.setWaitingCount?.(0,false)"),'Waiting Room count must stay synchronized with the native shell and clear when the meeting ends.');
assert(preload.includes("setWaitingCount:(count,attention=false)"),'Renderer must access waiting-room native attention only through the narrow notification bridge.');
assert(preload.includes("notifications:Object.freeze({showMeeting:"),'Renderer must access native meeting notifications only through a narrow bridge.');
assert(js.includes("new CustomEvent('dominion:meeting-snapshot'"),'Existing room snapshot polling must publish one shared local state event for captions and other meeting modules.');
assert(meetingCaptions.includes("version:'1.3.0'")&&meetingCaptions.includes("roomCaptions")&&meetingCaptions.includes("roomCaptionsMenu"),'Meeting shell must expose Zoom-style Show/Hide Captions plus a caption-options control.');
assert(meetingCaptions.includes('const cutoff=now()-180000')&&meetingCaptions.includes("Past 3 minutes"),'Live caption history must remain a short three-minute accessibility window rather than a retained transcript.');
assert(meetingCaptions.includes('setCaptionState')&&meetingCaptions.includes("add('I will type'")&&meetingCaptions.includes('Assign participant to type')&&meetingCaptions.includes("add('Assign participant'")&&meetingCaptions.includes("const canManage=()=>state.role==='host'"),'Caption setup must match Zoom host-only I-will-type and assign-participant flows through server-backed authority.');
assert(meetingCaptions.includes("add('Request Captions'")&&meetingCaptions.includes("meeting.sendSignal(p.participantId,'caption-request',payload)"),'Non-host participants must be able to request captions from the host through authenticated meeting signaling.');
assert(meetingCaptions.includes("if(type==='caption-request'){handleCaptionRequest(detail);return;}")&&meetingCaptions.includes('requested captions.'),'Host must receive and surface caption requests.');
assert(meetingCaptions.includes('Retain Meeting Transcript')&&meetingCaptions.includes('Download Retained Transcript'),'Retained transcript must remain a separate explicit host choice from live captions.');
assert(meetingCaptions.includes('Automated captions are not enabled in this QA build until a stable speech engine is certified.'),'Desktop must not expose fake automated captions before a stable speech engine is certified.');
assert(meetingCaptionsCss.includes('.meeting-caption-overlay')&&meetingCaptionsCss.includes('.meeting-caption-panel')&&meetingCaptionsCss.includes('.meeting-caption-menu'),'Live captions, full-caption panel, and caption options must have dedicated desktop styling.');
assert(preferences.includes('Closed Captioning')&&preferences.includes('captionFontSize')&&preferences.includes('captionFontType')&&preferences.includes('captionTheme')&&preferences.includes('captionPosition')&&preferences.includes('alwaysShowCaptions'),'Accessibility settings must expose Zoom-style personal caption display preferences.');
assert(meetingCaptions.includes("pref('captionFontSize'")&&meetingCaptions.includes("pref('captionFontType'")&&meetingCaptions.includes("pref('captionTheme'")&&meetingCaptions.includes("pref('captionPosition'"),'Live caption rendering must read personal accessibility preferences rather than meeting authority state.');
assert(meetingCaptions.includes("window.addEventListener('dominion:preference-change'"),'Caption appearance changes must apply immediately without restarting the meeting.');
assert(meetingCaptions.includes("overlay.classList.toggle('caption-popout'")&&meetingCaptionsCss.includes('.meeting-caption-overlay.caption-popout')&&meetingCaptionsCss.includes('resize:both'),'Caption Position must support pinned-bottom and movable/resizable pop-out overlay behavior.');
assert(meetingCaptions.includes("pref('alwaysShowCaptions',false)")&&preferences.includes('Always show captions when available'),'Always-show captions must remain a local user preference.');
assert(!preferences.includes('setCaptionState(')&&!preferences.includes('publishCaption('),'Personal Accessibility settings must never modify host caption/transcript authority.');

assert(shareService.includes('compactMainWindow'),'Desktop sharing must own a native compact meeting-window mode.');
assert(shareService.includes("main.on('minimize',mainMinimizeHandler)"),'Minimizing during a share must compact the meeting window instead of losing participant video.');
assert(shareService.includes("main.setAlwaysOnTop(true,'floating')"),'Compact participant video must remain above the presented content.');
assert(shareService.includes('main.setMinimumSize(300,190)'),'Share mode must temporarily release the normal full meeting minimum size.');
assert(shareService.includes('restoreMainWindowAfterShare'),'Stopping a share must restore the original meeting window geometry.');
assert(shareService.includes('setContentProtection'),'Presenter meeting chrome must request capture exclusion through Electron.');
assert(shareService.includes("meetingVisible:false")&&shareService.includes("meetingVisible:true"),'Presenter controls must track compact versus full meeting visibility.');
assert(shareCss.includes('@media(max-width:560px) and (max-height:360px)'),'Share UI must have a dedicated compact participant-panel layout.');
assert(shareCss.includes('.participant-video-dock.count-1'),'Compact video panel must adapt to the participant count.');
assert(presenterHtml.includes('<svg viewBox="0 0 24 24"'),'Presenter toolbar must use vector controls.');
for(const legacyGlyph of ['◉','▣','♙','▢','Ⅱ','✎','▤'])assert(!presenterHtml.includes(legacyGlyph),`Presenter toolbar must not regress to legacy glyph ${legacyGlyph}.`);
assert(presenterCss.includes('.icon svg'),'Presenter toolbar must explicitly style its vector icon system.');
assert(presenterJs.includes("state?.meetingVisible?'Hide meeting':'Show meeting'"),'Presenter toolbar must expose the real meeting-window visibility state.');

assert(packageJson.build?.appId==='com.dominionstar.desktop','Desktop rebuild must preserve the DominionStar macOS/Windows application identity.');
assert(packageJson.build?.productName==='DominionStar Meet','Desktop rebuild must preserve the DominionStar Meet product name.');
assert(packageJson.build?.mac?.icon==='build/icon.svg','macOS package must use the DominionStar Meet app icon.');
assert(packageJson.build?.win?.icon==='build/icon.svg','Windows package must use the DominionStar Meet app icon.');
assert(appIcon.includes('<title id="title">DominionStar Meet</title>'),'Branded desktop icon must identify DominionStar Meet.');
assert(appIcon.includes('geometric D monogram')&&appIcon.includes('url(#gold)')&&appIcon.includes('url(#orbit)'),'Desktop icon must retain the approved DominionStar D/star/orbit artwork.');

console.log('DOMINIONSTAR_DESKTOP_FOUNDATION_OK local-home four-primary-actions personal-room stable-identity passcode-3-7 recurring-schedules live-preferences no-dead-home-chrome responsive-controls real-share-entry isolated-share-module native-compact-share-window vector-presenter-controls branded-app-identity');
