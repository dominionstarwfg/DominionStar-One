import fs from 'node:fs';
import assert from 'node:assert/strict';
import { createShareSourceAuthority } from '../src/share-source-authority.mjs';

const read=rel=>fs.readFileSync(new URL(`../${rel}`,import.meta.url),'utf8');
const service=read('src/share-service.mjs');
const main=read('src/main.mjs');
const preload=read('src/preload.cjs');
const picker=read('ui/share-picker.js');
const pickerHtml=read('ui/share-picker.html');
const mediaController=read('ui/media-controller.js');
const controller=read('ui/share-controller.js');
const integration=read('ui/share-integration.js');
const toolbar=read('ui/presenter-toolbar.html');
const annotation=read('ui/share-annotation.js');
const media=read('ui/media-controller.js');

let enumerateCount=0;
let releaseFirst;
const firstEnumeration=new Promise(resolve=>{releaseFirst=resolve;});
const authority=createShareSourceAuthority({timeoutMs:20,enumerateSources:async()=>{enumerateCount+=1;if(enumerateCount===1)return firstEnumeration;return [{id:'screen:second'}];}});
const [first,second]=await Promise.all([authority.list(),authority.list()]);
assert.equal(enumerateCount,1,'Overlapping same-source requests must share one native enumeration.');
assert.equal(first.timedOut,true,'A stalled native source request must time out to the picker.');
assert.equal(second.timedOut,true,'Every waiter must be released when native source enumeration stalls.');
assert.equal(authority.busy(),true,'Timed-out source discovery must not stack another native request while the first is unresolved.');
releaseFirst([{id:'screen:first'}]);
await firstEnumeration;await new Promise(resolve=>setTimeout(resolve,0));
assert.equal(authority.busy(),false,'Native source authority must recover after the pending request completes.');
const recovered=await authority.list();
assert.equal(enumerateCount,2,'A later source refresh may start only after the original native request settles.');
assert.equal(recovered.ok,true);assert.equal(authority.get('screen:second')?.id,'screen:second');

const grouped=createShareSourceAuthority({timeoutMs:100,enumerateSources:async options=>String(options?.kind)==='window'?[{id:'window:one'}]:[{id:'screen:one'}]});
await grouped.list({kind:'screen'});await grouped.list({kind:'window'});
assert.equal(grouped.get('screen:one')?.id,'screen:one','Screen source must remain selectable after window enumeration.');
assert.equal(grouped.get('window:one')?.id,'window:one','Window source must remain selectable beside screen sources.');
assert.equal(grouped.snapshot().sourceCount,2,'Combined source authority must retain both source groups.');

assert(main.includes("systemPreferences.getMediaAccessStatus(kind)"),'macOS media status may remain available for diagnostics/settings.');
assert(main.includes("diagnosticStatus:permissionStatus('screen')"),'Screen Recording status must remain independently observable for diagnostics only.');
assert(main.includes("status:'native-source-authority'")&&main.includes("media:request-screen"),'Screen permission IPC must defer authority to native source discovery.');
assert(!/requestScreenPermission[\\s\\S]*?return \\{ok:false/.test(main),'A cached macOS TCC status must never block native source discovery.');
assert.equal((service.match(/setDisplayMediaRequestHandler/g)||[]).length,1,'Exactly one Electron display-media handler may own capture.');
assert(service.includes('{useSystemPicker:false}'),'Only one source-selection authority may own display capture.');
assert(service.includes("ipcMain.handle('share:open-picker',()=>openPicker())"),'Share must open the actual source picker without a stale TCC-status pre-gate.');
assert(!integration.includes('requestScreen?.()'),'Meeting renderer must not block Share on systemPreferences screen status before source discovery.');
assert(!service.includes("if(platform==='darwin'&&typeof ensureScreenPermission==='function')"),'Main-process Share must not block on stale getMediaAccessStatus screen state.');
assert(service.includes("permissionRequired:platform==='darwin'"),'Actual native source-enumeration failure must still expose macOS permission recovery.');
assert(!service.includes("openPrivacySettings('screen').catch"),'Permission failure must not automatically steal focus into System Settings.');
assert(service.includes("types:[kind]"),'Source authority must enumerate only one source class per native ScreenCaptureKit call.');
assert(service.includes("thumbnailSize:{width:360,height:203}"),'Share previews must be large enough for reliable visual source selection.');
assert(service.includes("fetchWindowIcons:false"),'Source discovery must avoid unnecessary window-icon work on the critical path.');
assert(picker.includes("kind:'screen'")&&picker.includes("kind:'window'"),'Picker must explicitly enumerate both Entire Screen and Application Windows groups.');
assert(picker.includes("const first=screens[0]||windows[0]||null"),'Picker must preselect the first available display/window so Share is immediately actionable.');
assert(pickerHtml.includes('Entire screen')&&pickerHtml.includes('Application windows'),'Functional picker must expose the same two core source groups proven on physical Zoom.');
assert(pickerHtml.includes('Share sound')&&pickerHtml.includes('Optimize for video sharing'),'Functional picker must expose real sound and video-optimization options.');
assert(!pickerHtml.includes('Presenter layout')&&!pickerHtml.includes('data-filter="files"'),'Picker must not advertise decorative layouts/files until those behaviors are implemented.');
assert(service.includes('timeoutMs:4500'),'Production source enumeration must be bounded.');
assert(service.includes('if(pickerWindow&&!pickerWindow.isDestroyed())'),'Share picker must be single-instance.');
assert(main.includes('createShareService({BrowserWindow,desktopCapturer'),'Electron main process must own source enumeration.');
assert(!preload.includes('desktopCapturer')&&!picker.includes('desktopCapturer')&&!integration.includes('desktopCapturer'),'Renderer surfaces must never enumerate native sources directly.');
assert(preload.includes('sharePicker:Object.freeze')&&preload.includes('presenter:Object.freeze'),'Picker and presenter controls must use narrow IPC bridges.');
assert(!picker.includes('showModal')&&!pickerHtml.includes('<dialog'),'Share chooser must be an independent window, not a blocking dialog inside the meeting renderer.');
assert.equal((mediaController.match(/script\.src='\.\/share-integration\.js'/g)||[]).length,1,'Media controller must own exactly one Share integration bootstrap path.');
assert(mediaController.includes("script.dataset.dsShareIntegration='1'"),'Share integration bootstrap must be explicitly marked/idempotent.');
assert(integration.includes("let button=overlay.querySelector('#roomShare')")&&integration.includes("button.id='roomShare'"),'Loaded share integration must create the in-meeting Share Screen control.');
assert(integration.includes('requestAnimationFrame(()=>setTimeout'),'Share click must release the meeting event loop before picker work starts.');
assert(integration.includes('openSharePicker'),'Meeting UI must have one direct source-picker entry point.');
assert((controller.match(/getDisplayMedia/g)||[]).length>=2,'Display capture authority must remain implemented inside the isolated share controller.');
assert(!integration.includes('getDisplayMedia')&&!preload.includes('getDisplayMedia')&&!picker.includes('getDisplayMedia'),'No renderer integration, preload bridge, or picker surface may acquire display media directly.');
assert(controller.includes("context.drawImage(videoElement,0,0,width,height)"),'Pause must freeze the exact last shared frame.');
assert(controller.includes('canvas.captureStream(1)'),'Pause must create a frozen presentation stream rather than showing black.');
assert(controller.includes('const baseOutputStream=()=>state.paused&&state.frozenStream?state.frozenStream:state.liveStream'),'Paused/unpaused base output must deterministically switch between frozen and live capture.');
assert(controller.includes("for(const audioTrack of state.liveStream.getAudioTracks?.()||[]){try{frozen.addTrack(audioTrack.clone())"),'Pause must freeze video while preserving shared computer audio.');
assert(controller.includes("track.contentHint=optimize?'motion':'detail'")&&controller.includes("audioTrack.contentHint='music'"),'Share capture must signal motion/detail and music-quality system audio intent.');
assert(controller.includes('state.paused=false;if(state.annotationCanvas)startComposite()'),'Resume must restore the live base and rebuild annotation composition when annotations are active.');
assert(controller.includes('function outputStream(){return state.annotationCanvas&&state.compositeStream?state.compositeStream:baseOutputStream();}'),'Presentation output must use live/frozen base unless annotation composition is active.');
assert(annotation.includes('setAnnotationCanvas'),'Annotation UI must attach its canvas through the share controller rather than a second capture authority.');
assert(annotation.includes('data-annotation-mode="laser"')&&annotation.includes('drawLaser')&&annotation.includes('clearLaser(650)'),'Annotation must provide a transient laser pointer.');
assert(annotation.includes('data-annotation-undo')&&annotation.includes('state.history.pop()')&&annotation.includes('getImageData'),'Annotation must provide bounded Undo history.');
for(const color of ['#ff3b30','#2d8cff','#28c76f','#ffffff'])assert(annotation.includes(`data-annotation-color="${color}"`),`Annotation color palette missing ${color}.`);
assert(controller.includes('stopTracks(state.liveStream)'),'Stop Share must release the live screen-capture track.');
assert(controller.includes('async function replaceSource')&&controller.includes('const previousLive=state.liveStream'),'New Share must have an explicit transactional source-replacement path.');
assert(controller.indexOf('const {stream,track}=await acquireDisplay(options);',controller.indexOf('async function replaceSource'))<controller.indexOf('stopTracks(previousFrozen);stopTracks(previousLive);',controller.indexOf('async function replaceSource')),'New Share must acquire replacement before releasing the current presentation.');
assert(integration.includes("if(replacing){await share.replaceSource")&&integration.includes("if(command==='new-share'){await openSharePicker();return;}"),'Presenter New Share must keep the current share active while the picker is open.');
assert(service.includes('if(toolbarWindow&&!toolbarWindow.isDestroyed()){toolbarWindow.showInactive();publishToolbarState();return;}'),'Presenter toolbar must remain single-instance across source changes.');
assert(toolbar.includes('shareSourceLabel')&&toolbar.includes('shareAudioFlag')&&toolbar.includes('shareOptimizeFlag'),'Presenter toolbar must expose current source, Computer Sound, and Optimize-for-Video state.');
assert(integration.includes('shareAudio:Boolean(state.options?.shareAudio)')&&integration.includes('optimizeVideo:Boolean(state.options?.optimizeVideo)'),'Presenter toolbar state must be driven from real active share options.');
assert(service.includes('shareAudio:false,optimizeVideo:false'),'Presenter toolbar shell must reset share-option state when sharing stops.');
for(const command of ['audio','video','pause','participants','show-meeting','stop'])assert(toolbar.includes(`data-command="${command}"`),`Presenter toolbar is missing ${command}.`);
assert(media.includes("script.src='./share-integration.js'"),'Desktop and Netlify must load the same isolated share integration.');
assert(!integration.includes('showModal'),'Meeting share integration must never create a blocking modal.');
console.log('DOMINIONSTAR_SHARE_AUTHORITY_OK native-source-authority no-stale-TCC-gate combined-screen-window-picker single-flight bounded transactional-new-share system-audio optimize-video presenter-state pause-freeze live-resume annotation-composite laser undo colors stop single-floating-toolbar');
